services:
  back:
    image: woupa784/my-back:latest
    ports:
      - "5000:5000"
    environment:
      - CURRENT_ENVIRONMENT=development
    # volumes:
    #   - ./logs:/app/logs
  front:
    image: woupa784/my-front:latest
    ports:
      - "8080:80"
    environment:
      - WS_BACK_URL=http://localhost:5000